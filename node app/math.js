
let sum = (a, b) => console.log(`The sum of ${a} and ${b} is ${a + b}`)

let sub = (a, b) => console.log(`The difference of ${a} and ${b} is ${a - b}`)

// export default sum;

export {
    sum,
    sub
}